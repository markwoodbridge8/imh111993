import WalletConnect from "@walletconnect/client";
import { SignerTransaction } from "./util/model/daffiWalletModels";
import { AlgorandChainIDs } from "./util/daffiWalletTypes";
interface DaffiWalletConnectOptions {
    bridge?: string;
    shouldShowSignTxnToast?: boolean;
    chainId?: AlgorandChainIDs;
}
declare class DaffiWalletConnect {
    bridge: string;
    connector: WalletConnect | null;
    shouldShowSignTxnToast: boolean;
    chainId?: number;
    constructor(options?: DaffiWalletConnectOptions);
    get platform(): import("./util/daffiWalletTypes").DaffiWalletPlatformType;
    get isConnected(): boolean;
    private connectWithWebWallet;
    connect(): Promise<string[]>;
    reconnectSession(): Promise<string[]>;
    disconnect(): Promise<void>;
    private signTransactionWithMobile;
    private signTransactionWithWeb;
    signTransaction(txGroups: SignerTransaction[][], signerAddress?: string): Promise<Uint8Array[]>;
}
export default DaffiWalletConnect;
